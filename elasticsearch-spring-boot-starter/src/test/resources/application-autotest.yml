spring:
  application:
    name: elasticsearch-starter-test
  main:
    allow-bean-definition-overriding: true
  data:
    elasticsearch:
      repositories:
        enabled: true

  elasticsearch:
    uris: https://ubuntu.wsl:9200
    connection-timeout: 10s
    socket-timeout: 30s
    username: elastic
    password: elastic


pg:
  elasticsearch:
    rest:
      ssl: true



logging:
  level:
    root: info
    net.engining: trace

