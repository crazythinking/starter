spring:
  main:
    allow-bean-definition-overriding: true
  shardingsphere:
    enabled: false

pg:
  datasource:
    dynamic:
      druid:
        enabled: false
      hikari:
        enabled: true

logging:
  level:
    root: info
    net.engining: trace
    org.springframework.boot: info

