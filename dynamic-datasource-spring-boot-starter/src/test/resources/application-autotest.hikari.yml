spring:
  main:
    allow-bean-definition-overriding: true
  shardingsphere:
    enabled: false

pg:
  datasource:
    dynamic:
      druid:
        enabled: false
      hikari:
        enabled: true

gm:
  common:
    async-excutor-thread-name-prefix: db-event-listener-
